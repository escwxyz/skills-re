---
interface Props {
  totalSkills: string
}

const { totalSkills } = Astro.props
---

<section
  class="grid grid-cols-1 gap-8 border-b border-border pt-6 pb-2 md:grid-cols-[3fr_2fr]"
>
  <div class="border-b border-border pb-8 md:border-b-0 md:border-r md:pb-0 md:pr-8">
    <div class="mb-2 flex items-center gap-2.5">
      <span class="inline-block h-2 w-2 rounded-full bg-destructive"></span>
      <span class="font-mono text-[10.5px] tracking-[.2em] uppercase text-destructive">
        Leading — Editor's Dispatch
      </span>
    </div>
    <h2
      class="font-display my-1 mb-4 text-[clamp(40px,6vw,74px)] font-normal leading-[.94] tracking-[-0.015em]"
    >
      Not a package. <em>A practice.</em><br />A skill is an <em>instruction</em>, not an
      install.
    </h2>
    <p class="mb-5 max-w-155 font-serif text-[20px] leading-[1.4] text-ink-2">
      skills.re is an open reference catalogue of agent skills — small, composable bundles
      of prose, schema, and scaffolding that teach a language model <em
        >how to do one thing well.</em
      > Discover them. Audit them. Sandbox them. Then hand the right one to the right agent.
    </p>
    <div class="mt-4.5 mb-2.5 flex flex-wrap gap-3">
      <a href="/registry" class="btn">Browse the Index <span>→</span></a>
      <a href="/submit" class="btn ghost">Publish a skill</a>
    </div>
    <div class="mt-6.5 font-mono text-[10.5px] tracking-[.14em] uppercase text-muted-foreground">
      <b class="font-medium text-foreground">By the Registry Desk</b> — with dispatches from
      the Sandbox and the Evals Bench. Filed <b class="font-medium text-foreground"
        >04/19/2026 — 06:41 UTC.</b
      >
    </div>
  </div>

  <div class="grid content-start gap-3.5">
    <div class="ph aspect-4/3">
      <span class="ph-label">Figure 01 — skill graph</span>
    </div>
    <div class="font-serif text-[13px] italic leading-[1.4] text-muted-foreground">
      <b class="not-italic font-mono text-[10.5px] tracking-[.14em] uppercase text-foreground mr-1.5">
        Fig. 01
      </b>A constellation of {totalSkills} skills, rendered by their call graph. Dense nodes
      are <em>code-review</em> and <em>research-synthesis</em>; peripheral clusters are
      domain-specific.
    </div>
  </div>
</section>
