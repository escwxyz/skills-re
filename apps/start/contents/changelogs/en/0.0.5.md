---
title: Keyword Search Fix
description: CLI search now uses the keyword search path by default, matching the FTS5-backed registry search and avoiding the AI search rate limit.
type: patch
versionMajor: 0
versionMinor: 0
versionPatch: 5
isPublished: true
isStable: false
date: 2026-07-26
changes:
  - CLI search now requests keyword search instead of AI semantic search
  - Keyword search requests bypass the anonymous AI search rate limit
  - CLI search help and documentation now describe keyword-based discovery
  - Search API contract documentation now distinguishes keyword and semantic modes
---

The CLI now follows the same keyword search path used by the registry search experience. This keeps terminal search predictable, avoids consuming the stricter anonymous AI search quota, and prepares the next CLI release to use FTS5-backed keyword results by default.
